package pages.guest_book;

import org.openqa.selenium.WebDriver;

public class ReadGuestbookPage extends GuestbookSubmenuPage {

    public ReadGuestbookPage(WebDriver driver) {
        super(driver);
    }
}
